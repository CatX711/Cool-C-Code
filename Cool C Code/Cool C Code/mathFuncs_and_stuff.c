#include <stdio.h>
#include <math.h>

int main(){

    double A = sqrt(9); // square root of 9
    double B = pow(2, 4); // raises 2 to the power of 4 (output = 16)
    int C = round(3.14); // self explanitory
    int D = ceil(3.14); // always rounds number up
    int E = floor(3.99); // always rounds down
    double F = fabs(-100.0); // finds absolute value of floating point nums (how far num is from 0) output = 100.000000
    double G = log(3); // finds logarithm of num
    double H = sin(45); // sine
    double I = cos(45); // cosine
    double J = tan(45); // tangent

    printf("\n%lf", F);




    return 0;
}